import { NepaliUnicodeDirective } from './nepali-unicode.directive';

describe('NepaliUnicodeDirective', () => {
  it('should create an instance', () => {
    const directive = new NepaliUnicodeDirective();
    expect(directive).toBeTruthy();
  });
});
