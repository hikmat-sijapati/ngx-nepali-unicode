import { NepaliUnicodePipe } from './nepali-unicode.pipe';

describe('NepaliUnicodePipe', () => {
  it('create an instance', () => {
    const pipe = new NepaliUnicodePipe();
    expect(pipe).toBeTruthy();
  });
});
